public enum ToppingType {

    // shout out to paulo explaining that enums is by default a static class on its own so it uses it's own file.

    MEAT, CHEESE, REGULAR, SAUCE // enums to use for case switch for the get price method
}
